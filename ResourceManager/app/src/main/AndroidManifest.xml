<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mediatecnica.pjic.resourcemanager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".inicio" />
        <activity android:name=".registro" />
        <activity android:name=".Catalogo" />
        <activity android:name=".Clientes" />
        <activity android:name=".actualizar_cliente" />
        <activity android:name=".Agregar_producto" />
        <activity android:name=".Actualizar_producto" />
        <activity android:name=".AgregarCliente" />
        <activity android:name=".Venta" />
        <activity android:name=".Ventas"></activity>
    </application>

</manifest>